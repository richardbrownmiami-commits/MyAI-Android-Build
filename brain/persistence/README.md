# Persistence

SQLite is the durable storage layer.

AtomSpace and ONA are runtime cognitive components; SQLite is not treated as the cognitive representation itself.

The persistence API will eventually provide:
- save/load AtomSpace state
- save/load ONA state
- provenance records
- schema migrations
- brain export/import
